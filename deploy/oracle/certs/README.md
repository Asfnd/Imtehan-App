# Orange-cloud DNS: mount Cloudflare Origin Certificate as origin.crt + origin.key here.
# Cloudflare → SSL/TLS → Origin Server → Create Certificate (imtehan.com, *.imtehan.com)
# Then use Caddyfile.cloudflare-origin instead of Caddyfile.
#
# Do NOT commit real certificate files.
