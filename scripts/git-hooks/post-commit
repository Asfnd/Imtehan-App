#!/usr/bin/env bash
# Push to GitHub after every commit in this repo.
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
branch="$(git branch --show-current)"

if git remote get-url origin >/dev/null 2>&1; then
  git push origin "$branch" >>"$ROOT/auto-push.log" 2>&1 || true
fi
